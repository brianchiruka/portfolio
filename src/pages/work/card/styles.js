import { makeStyles } from "@material-ui/core";

export default makeStyles((theme) => ({
  project: {
    height: 650,
    width: 450,
    borderRadius: 20,
    paddingTop: 60,
    paddingLeft: 30,
    paddingRight: 30,
  },
  projectTitle: {
    fontFamily: "DR-RAYMOND Display",
  },
}));
